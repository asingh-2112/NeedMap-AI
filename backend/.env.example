# ── App ─────────────────────────────────────────────────────────
APP_NAME=NeedMap AI
APP_VERSION=0.1.0

# ── Database ────────────────────────────────────────────────────
DATABASE_URL=

# ── JWT Auth ────────────────────────────────────────────────────
JWT_SECRET_KEY=adasdasdasdasdasddasdasd
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=60

# ── Gemini — Vertex AI (primary) + AI Studio (fallback) ──────
# Vertex AI via service account (preferred — $300 free credit)
GOOGLE_APPLICATION_CREDENTIALS=service-account.json
# AI Studio API key (fallback — get at https://aistudio.google.com/apikey)
GEMINI_API_KEY=
LLM_MODEL=gemini-2.5-flash