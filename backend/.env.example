# ── App ─────────────────────────────────────────────────────────
# Everything below is optional — secrets live in Google Secret Manager.
# The service account JSON is auto-detected at backend/service-account.json.
# DATABASE_URL, JWT_SECRET_KEY, GEMINI_API_KEY are fetched from Secret Manager.

APP_NAME=NeedMap AI
APP_VERSION=0.1.0
LLM_MODEL=gemini-2.5-flash